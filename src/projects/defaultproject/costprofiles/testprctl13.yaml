apiVersion: cost.k8smgmt.io/v3
kind: CostProfile
metadata:
  name: testprctl13
  project: defaultproject
spec:
  installationParams:
    aws:
      awsCredentials:
        awsServiceKeyName: sdf
      curIntegration: {}
      spotIntegration:
        spotLabel: sa
  providerType: AWS
  sharing:
    enabled: false
  version: v16
