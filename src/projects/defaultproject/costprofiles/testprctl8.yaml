apiVersion: cost.k8smgmt.io/v3
kind: CostProfile
metadata:
  name: testprctl8
  project: defaultproject
spec:
  installationParams:
    aws:
      awsCredentials: {}
      curIntegration: {}
      spotIntegration:
        spotLabel: sa
  providerType: CUSTOM
  sharing:
    enabled: false
  version: v16
