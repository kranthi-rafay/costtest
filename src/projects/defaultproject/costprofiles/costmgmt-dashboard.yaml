apiVersion: cost.k8smgmt.io/v3
kind: CostProfile
metadata:
  name: costmgmt-dashboard
  project: defaultproject
spec:
  installationParams:
    aws:
      spotIntegration:
        spotLabel: spot
        spotLabelValue: "true"
  providerType: AWS
  sharing:
    enabled: false
  version: v2
